from pathlib import Path
import sys

sys.path.insert(0, str(Path(__file__).resolve().parent))

from rife_vpy_common import run_rife_profile

clip = run_rife_profile(video_in, model="4.26", scale=1.0, factor_num=2, factor_den=1)
clip.set_output()
