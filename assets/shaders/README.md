Optional shader assets live here.

Windows defaults to NVIDIA driver-level RTX Video Super Resolution through the
D3D11 mpv path. GLSL upscalers are not enabled by default. Put
FSRCNNX_x2_8-0-4-1.glsl in this directory only if you explicitly want the
installer to copy it with -EnableGlslUpscaleFallback for debugging.
